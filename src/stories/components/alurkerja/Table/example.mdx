import { Meta, Source } from '@storybook/blocks'
import * as TableStories from './TableLowcode.stories'

<Meta of={TableStories} />

<Source code={
  `
    import { TableLowcode } from 'alurkerja-ui'

    const Example = () => {

        const baseUrl="https://kpm-sys.merapi.javan.id"
        const tableName="jpn"

        return(
          <TableLowcode
            baseUrl={baseUrl}
            tableName={tableName}
            customCell={({ name, fields, value, defaultCell }) => {
              if (name === 'created_at') {
                return <td className="text-red-400 text-center">custom Cell</td>
              } else {
                return defaultCell
              }
            }}
          />
        )
    }

`
} />
